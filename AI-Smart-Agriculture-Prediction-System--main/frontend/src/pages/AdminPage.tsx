import { FormEvent, useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { agricultureApi } from '../api'; import '../feature.css'
export default function AdminPage(){const[analytics,setAnalytics]=useState<any>(null),[users,setUsers]=useState<any[]>([]),[message,setMessage]=useState(''),[file,setFile]=useState<File|null>(null),[name,setName]=useState(''),[domain,setDomain]=useState('');useEffect(()=>{Promise.all([agricultureApi.adminAnalytics(),agricultureApi.adminUsers()]).then(([a,u])=>{setAnalytics(a.data);setUsers(u.data)}).catch(()=>setMessage('Administrator access is required.'))},[]);const upload=async(e:FormEvent)=>{e.preventDefault();if(!file)return;try{const response=await agricultureApi.dataset(name,domain,file);setMessage(`Dataset uploaded: ${response.data.rows} cleaned rows. Enter its target column to train through the API.`)}catch(err:any){setMessage(err.response?.data?.detail??'Upload failed.')}};return <main className="feature"><Link className="back" to="/dashboard">← Dashboard</Link><p className="eyebrow">ADMINISTRATION</p><h1>Platform control center</h1>{analytics&&<div className="admin-stats">{Object.entries(analytics).map(([key,value])=><div key={key}><strong>{String(value)}</strong><span>{key.replace('_',' ')}</span></div>)}</div>}<div className="feature-grid"><form className="tool-form" onSubmit={upload}><h2>Upload training dataset</h2><label>Dataset name<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>Domain<input required value={domain} onChange={e=>setDomain(e.target.value)} placeholder="yield"/></label><label>CSV file<input required type="file" accept=".csv" onChange={e=>setFile(e.target.files?.[0]??null)}/></label><button>Validate and upload</button>{message&&<p className="success">{message}</p>}</form><article className="result-panel"><h2>Recent users</h2><div className="user-list">{users.map(user=><p key={user.id}><strong>{user.name}</strong><br/><small>{user.email} · {user.role}</small></p>)}</div></article></div></main>}
