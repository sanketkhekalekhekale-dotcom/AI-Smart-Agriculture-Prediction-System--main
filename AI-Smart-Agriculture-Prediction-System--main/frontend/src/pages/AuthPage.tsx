import { FormEvent, useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { motion } from 'framer-motion'
import { FaLeaf, FaArrowRight } from 'react-icons/fa'
import { useAuth } from '../AuthContext'
export default function AuthPage({ mode }: { mode: 'login' | 'register' }) {
 const register = mode === 'register', auth = useAuth(), nav = useNavigate(); const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [pending, setPending] = useState(false)
 const submit = async (e: FormEvent) => { e.preventDefault(); setError(''); setPending(true); try { register ? await auth.register(name, email, password) : await auth.login(email, password); nav('/dashboard') } catch (err: any) { setError(err.response?.data?.detail ?? 'Unable to authenticate. Please try again.') } finally { setPending(false) } }
 return <main className="auth-shell"><section className="brand"><div className="brand-mark"><FaLeaf/></div><p className="eyebrow">SMART AGRICULTURE</p><h1>Grow with<br/><i>confidence.</i></h1><p>Clear, AI-informed decisions for every season.</p></section><motion.section className="auth-card" initial={{opacity:0,y:18}} animate={{opacity:1,y:0}}><h2>{register ? 'Create your account' : 'Welcome back'}</h2><p className="sub">{register ? 'Start building a stronger harvest today.' : 'Sign in to your farm intelligence hub.'}</p><form onSubmit={submit}>{register && <label>Full name<input required minLength={2} value={name} onChange={e=>setName(e.target.value)} placeholder="Your full name"/></label>}<label>Email address<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters"/></label>{!register && <Link className="muted-link" to="/forgot-password">Forgot password?</Link>}{error && <p className="error">{error}</p>}<button disabled={pending}>{pending ? 'Please wait…' : register ? 'Create account' : 'Sign in'} <FaArrowRight/></button></form><p className="switch">{register ? 'Already have an account?' : 'New to AgriSense?'} <Link to={register ? '/login' : '/register'}>{register ? 'Sign in' : 'Create account'}</Link></p></motion.section></main>
}
