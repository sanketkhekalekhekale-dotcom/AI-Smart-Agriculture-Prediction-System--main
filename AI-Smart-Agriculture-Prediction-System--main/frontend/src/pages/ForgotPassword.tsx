import { FormEvent, useState } from 'react'
import { Link } from 'react-router-dom'
import { authApi } from '../api'
export default function ForgotPassword() { const [email,setEmail]=useState(''); const [message,setMessage]=useState(''); const [error,setError]=useState(''); const submit=async(e:FormEvent)=>{e.preventDefault();setError('');try{const r=await authApi.forgot(email);setMessage(r.data.message)}catch{setError('Unable to request a reset. Please try again.')}}; return <main className="auth-shell"><section className="brand"><p className="eyebrow">ACCOUNT RECOVERY</p><h1>Let’s get you<br/><i>growing again.</i></h1></section><section className="auth-card"><h2>Reset password</h2><p className="sub">Enter your email and we’ll send reset instructions.</p><form onSubmit={submit}><label>Email address<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} /></label>{message&&<p className="success">{message}</p>}{error&&<p className="error">{error}</p>}<button>Request reset</button></form><p className="switch"><Link to="/login">Back to sign in</Link></p></section></main> }
